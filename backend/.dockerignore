# Secrets never enter the image — Render and `docker run --env-file` supply
# configuration as real environment variables at runtime.
.env
.env.*

.venv
__pycache__
*.pyc
*.pyo
*.pyd
.python-version
uploads/
.git
.gitignore
.render-service-id.*
*.ps1
