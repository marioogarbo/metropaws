"""The MetroPaws API application.

Everything the running service needs lives under this package; the backend
directory around it holds project files (Dockerfile, pyproject.toml, deploy and
run scripts) and the two things that are not the app: scripts/ and tests/.

Entry point is app.main:app.
"""
